import { Container, Box, Heading, Text, Image, Button } from 'theme-ui';
import Link from 'next/link';

const Seminars = () => {
    return (
        <Link href="/e-seminars"><Button variant="primary">E-Surplus Seminars</Button></Link>
        )
}

export default Seminars;
